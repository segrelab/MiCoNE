title = "Normalize OTU table"

order = ["otu_processing.transform.normalize"]

[otu_processing.transform.normalize]
  module = "internal"
  [[otu_processing.transform.normalize.input]]
    datatype = "otu_table"
    format = ["biom"]
    location = "otus/biom/good/saliva.biom"
