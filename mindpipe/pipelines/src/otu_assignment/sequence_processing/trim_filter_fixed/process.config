export_visualization {
    seq_sample_size = {{ export_visualization['seq_sample_size'] }}
}

trim {
    ncpus = {{ trim['ncpus'] }}
}

params {
    sequence_16s = "{{ input['sequence_16s'] }}"
    sample_sequence_manifest = "{{ input['sample_sequence_manifest'] }}"
    output_dir = "{{ output_dir }}"

    seq_sample_size = export_visualization.seq_sample_size
    ncpus = trim.ncpus
}

process.conda = "{{ env }}"
