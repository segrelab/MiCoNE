dada2 {
    n_threads = {{ dada2['n_threads'] }} // 4
    max_ee = {{ dada2['max_ee'] }} // 2.0
    trunc_q = {{ dada2['trunc_q'] }} // 2
}

params {
    output_dir = "{{ output_dir }}"
    sequence_artifacts = "{{ input['qiime2_demultiplexed_sequences'] }}"

    n_threads = dada2.n_threads
    max_ee = dada2.max_ee
    trunc_q = dada2.trunc_q
}
