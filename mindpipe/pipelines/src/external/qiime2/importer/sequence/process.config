sequence_importer {
    input_type = "{{ sequence_importer['input_type'] }}"
}

params {
    output_dir = "{{ output_dir }}"
    input_folder = "{{ input['dir'] }}"

    input_type = sequence_importer.input_type
}

process.conda = "{{ env }}"
