sparcc {
  iterations = {{ sparcc['iteractions'] }}
  bootstraps = {{ sparcc['bootstraps'] }}
  threads = {{ sparcc['threads'] }}
}

params {
  output_dir = {{ output_dir }}
  otudata = {{ input['otu_table'] }}

  iterations = sparcc.iterations
  bootstraps = sparcc.bootstraps
}
