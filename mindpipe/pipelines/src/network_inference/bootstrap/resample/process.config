resample {
    bootstraps = {{ resample['bootstraps'] }}
    threads = {{ resample['threads'] }}
}

params {
    output_dir = "{{ output_dir }}"
    otudata = "{{ input['otu_table'] }}"

    bootstraps = resample.bootstraps
    threads = resample.threads
}

process.conda = "{{ env }}"
