resample {
    bootstraps = {{ resample['bootstraps'] }}
    ncpus = {{ resample['ncpus'] }}
}

params {
    output_dir = "{{ output_dir }}"
    otudata = "{{ input['otu_table'] }}"

    bootstraps = resample.bootstraps
    ncpus = resample.ncpus
}

process.conda = "{{ env }}"
