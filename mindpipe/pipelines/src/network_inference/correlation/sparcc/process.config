sparcc {
  iterations = {{ sparcc['iterations'] }}
  threads = {{ sparcc['threads'] }}
}

params {
  output_dir = "{{ output_dir }}"
  otudata = "{{ input['otu_table'] }}"

  iterations = sparcc.iterations
  threads = sparcc.threads
}

process.conda = "{{ env }}"
