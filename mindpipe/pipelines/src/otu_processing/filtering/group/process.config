group_by_taxa {
    tax_levels = "{{ group_by_taxa['tax_levels'] }}"
}

params {
    output_dir = "{{ output_dir }}"
    otudata = "{{ input['otu_table'] }}"
    tax_levels = group_by_taxa.tax_levels
}
